<%= form_with(model: comment) do |form| %>


 <%= form.hidden_field :post_id %>

 <%= form.label :text, "Comments:" %>
 <%= form.text_area :text %>

 <div class="actions">
  <%= form.submit %>
 </div>
<% end %>
